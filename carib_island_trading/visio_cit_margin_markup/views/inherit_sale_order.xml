<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_sale_order_form_margin_inherit" model="ir.ui.view">
        <field name="name">sale.order.form.margin.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <!-- Add Margin after existing Markup field (if exists) -->
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="margin_percent"/>
            </xpath>

            <!-- Add Margin column inside order lines -->
            <xpath expr="//field[@name='order_line']/list/field[@name='price_unit']" position="after">
                <field name="margin_percent_line"/>
            </xpath>


        </field>
    </record>


</odoo>
